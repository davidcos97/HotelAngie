NODE_ENV=development
PORT=4000

DATABASE_URL="postgresql://postgres:postgres@localhost:5432/coliving614"

JWT_SECRET="change-this-to-a-long-random-string"
JWT_EXPIRES_IN=7d

CORS_ORIGIN=http://localhost:3000

# SMTP (opcional — sin configurar, los correos solo se registran en consola)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=reservas@614coliving.com

# Contraseña del usuario admin creado por el seed
SEED_ADMIN_PASSWORD=Admin614!
