# Editor / OS
.DS_Store
Thumbs.db
.vscode/
.idea/

# Env files (each app also has its own .gitignore, this is a safety net)
.env
.env.local
.env.*.local

# Claude Code local settings (machine-specific permissions cache)
.claude/settings.local.json
